# Prevent dependency confusion attacks by ensuring packages come from public npm
registry=https://registry.npmjs.org/

# Scoped packages should also use public npm (not a private registry)
@trustatlas:registry=https://registry.npmjs.org/
